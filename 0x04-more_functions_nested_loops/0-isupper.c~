#include "main.h"

/**
 * _isupper - checks for uppercase char
 * @c: checks if c is uppercase
 *
 * return: Valuued 0
 */

int _isupper(int c)
{

	if (c >= A && c <= Z)
	{
		return (1);
	}

	return (0);



}
