#include "main.h"

/**
 * main - The main function
 * @argc: Number of arguments
 * @argv: Argument vector
 *
 * Return: 0
 */

int main(int argc, char **argv)
{
	int i;
	char c;

	for (i = 0; i < argc; i++)
	{
		c == argv[i];
		return (c);
	}


}
