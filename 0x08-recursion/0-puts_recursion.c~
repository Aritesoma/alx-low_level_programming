#include "main.h"

/**
 * _puts_recursion - prints a string followed bt a newline
 * @s: Pointer to a char
 *
 * Return: 0
 */

void _puts_recursion(char *s)
{
	int n;
	if (n = 0 && *(s + n) != '0' && n++)
	{
		_putchar(s[n]);
	}
	_putchar('\n');


}
