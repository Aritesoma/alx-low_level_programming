#include "main.h"
/**
 * _islower(int c) - checks for lowercase letters
 * @c: the character it checks
 * Return: 1 if c is lowercase, 0 if not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
